# 0.3.15

- MEGA line creation supports the current page where test-huberyliu is already selected, including hidden native catalogs and the newer bouquet-card layout.
- A MEGA selector or validation failure before Create Subscription is clicked can be retried safely; only a confirmed dispatched write can enter review.
- The designated test contact can archive a stale MEGA review left by an earlier executor database and continue a new acceptance test without replaying the old write.
- Ordinary customers still keep the one-trial-per-server rule and cannot clear or bypass unresolved operations.
- Chrome extension 0.4.15 suppresses only the DINO/TREX DataTables invalid-JSON alert and retries account readback without clicking Create a second time.
- Existing customer databases, settings, AI keys, server configuration and WhatsApp sessions remain preserved during update.
- Customer packages keep the protected runtime, protected browser assets and obfuscated Chrome extension without source maps.
